import {
  Meta,
  Canvas,
  Status,
  Story,
  Link,
  action,
} from '@pontte/stargate-ui-playground';
import {
  Typography,
  SvgIcon,
  Label,
} from '@pontte/stargate-ui-core';
import { Close as SvgIconClose } from '@pontte/stargate-ui-icons';

import Input from './Input';

export const InputStory = (props) => (
  <Input
    label="Cool input"
    onClear={action('onClear')}
    onChange={action('onChange')}
    {...props}
  />
);

<Meta title="Core/Form Elements/Input" component={Input} />

<Story
  name="Explore Properties"
  parameters={{
    docs: { disable: true }
  }}
>
  {InputStory.bind({})}
</Story>

## Input

<Status type="rc" />

### Basic

<Canvas>
  <Input
    label="Default"
    placeholder="Placeholder"
  />
  <Input
    label="Disabled"
    placeholder="Placeholder"
    disabled
  />
</Canvas>

### States

<Canvas>
  <Input
    label="Input with success state"
    placeholder="Placeholder"
    color="success"
  />
  <Input
    label="Input with warning state"
    placeholder="Placeholder"
    color="warning"
  />
  <Input
    label="Input with error state"
    placeholder="Placeholder"
    color="error"
  />
</Canvas>

### Using Icons And Ornaments

<Canvas>
  <Input
    label="Icon at start of input"
    componentAtStart={<SvgIconClose />}
  />
  <Input
    label="Icon at end of input"
    componentAtEnd={<SvgIconClose />}
  />
</Canvas>

### Custom Label

You can use any node element for `label` property. We recommended use <Link kind="core-label--page">Label</Link> component.

<Canvas>
  <Input
    label={
      <Label>
        <SvgIcon children={<path d="M6 19c0 1.1.9 2 2 2h8c1.1 0 2-.9 2-2V7H6v12zM19 4h-3.5l-1-1h-5l-1 1H5v2h14V4z" fill="currentColor" />} />
        Label customized
      </Label>
    }
    placeholder="Placeholder"
    color="success"
  />
</Canvas>

### Clear Option

<Canvas>
  <Input
    label="Input with clear option"
    value="Click in clear button to clear me!"
    placeholder="Placeholder"
    clear
  />
</Canvas>
