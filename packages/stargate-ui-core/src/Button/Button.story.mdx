import {
  Meta,
  Canvas,
  Status,
  Story,
  action,
} from '@pontte/stargate-ui-playground';
import { Typography } from '@pontte/stargate-ui-core';

import Button from './Button';

export const ButtonStory = (props) => (
  <Button
    onClick={action('onClick')}
    children="Cool button"
    color="primary"
    contained
    {...props}
  />
);

<Meta title="Core/Button" component={Button} />

<Story
  name="Explore Properties"
  parameters={{
    docs: { disable: true }
  }}
>
  {ButtonStory.bind({})}
</Story>

## Button

<Status type="rc" />

### Contained Button

<Canvas>
  <Button contained>Default</Button>
  <Button contained color="primary">Primary</Button>
  <Button contained color="secondary">Secondary</Button>
  <Button contained disabled>Disable</Button>
</Canvas>

### Outlined Button

<Canvas>
  <Button outlined>Default</Button>
  <Button color="primary" outlined>Primary</Button>
  <Button color="secondary" outlined>Secondary</Button>
  <Button outlined disabled>Disable</Button>
</Canvas>

### Text Button

<Canvas>
  <Button>Default</Button>
  <Button color="primary">Primary</Button>
  <Button color="secondary">Secondary</Button>
  <Button disabled>Disable</Button>
</Canvas>

### Sizes

<Canvas>
  <Button color="primary" contained>Small</Button>
  <Button
    color="primary"
    contained
    large
  >Large</Button>
</Canvas>
