import {
  Meta,
  Canvas,
  Status,
  Link,
  Story,
} from '@pontte/stargate-ui-playground';
import { Close as SvgIconClose } from '@pontte/stargate-ui-core';

import Label from './Label';

export const LabelStory = (props) => (
  <Label {...props}>
    <SvgIconClose /> Lorem ipsum dolor
  </Label>
);

<Meta title="Core/Label" component={Label} />

<Story
  name="Explore Properties"
  parameters={{
    docs: { disable: true },
    jest: ['Label.spec.jsx'],
  }}
>
  {LabelStory.bind({})}
</Story>

## Label

<Status type="rc" />

### Basic Usage

You can use `<Label>` with any node element.

<Canvas>
  <Label>Simple label</Label>
  <Label>
    <SvgIconClose />
    Label with <Link kind="core-svgicon--page">SvgIcon</Link> component
  </Label>
</Canvas>
