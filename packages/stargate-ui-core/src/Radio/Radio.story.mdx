import {
  Meta,
  Canvas,
  Status,
  Story,
  withAction,
  Props,
} from '@pontte/stargate-ui-playground';

import {
  Label,
  Instagram as SvgIconInstagram,
} from '@pontte/stargate-ui-core';

import Radio from './Radio';
import RadioGroup from './RadioGroup';
import Form from '../Form';

<Meta title="Core/Form Elements/Radio" component={Radio} />

# Radio Button

<Status type="rc" />

## Basic Usage

<Canvas>
  <Story name="Basic Usage" parameters={{
    jest: ['Radio.spec.jsx'],
  }}>
    {
      () => {
        const [value, setValue] = React.useState('radio-2');
        const handleChange = (event, [v]) => {
          setValue(v);
        };
        return (
          <RadioGroup
            name="radion-group-1"
            value={value}
            onChange={handleChange}
          >
            <Radio
              label="Radio 1"
              value="radio-1"
            />
            <Radio
              label="Radio 2"
              value="radio-2"
            />
            <Radio
              label="Radio Disabled"
              disabled
              value="radio-3"
            />
            <Radio
              label="Radio Readonly"
              disabled
              value="radio-4"
            />
          </RadioGroup>
        );
      }
    }
  </Story>
</Canvas>

<Props component={Radio} />

## Colors

<Canvas>
  <Story name="Colors">
    <Radio
      label="Success color"
      color="success"
      checked
    />
    <Radio
      label="Warning color"
      color="warning"
      checked
    />
    <Radio
      label="Error color"
      color="error"
      checked
    />
    <Radio
      label="Info color"
      color="info"
      checked
    />
  </Story>
</Canvas>

## Using with label

You can use any node element for `label` property. For more info, refer to Label component.

<Canvas>
  <Story name="Using with label">
    {
      () => {
        const [value, setValue] = React.useState('radio-2');
        const handleChange = (event, [v]) => {
          setValue(v);
        };
        return (
          <>
            <Label>Lorem ipsum dolor sit amet consectetur:</Label>
            <RadioGroup
              name="radion-group-1"
              value={value}
              onChange={handleChange}
            >
              <Radio
                label="Radio 1"
                value="radio-1"
              />
              <Radio
                label="Radio 2"
                value="radio-2"
              />
            </RadioGroup>
          </>
        );
      }
    }
  </Story>
</Canvas>
